-- Preview and render Markdown directly in the terminal
return {
  "ellisonleao/glow.nvim",
  enabled = false,
  opts = {
    style = "dark",
    pager = false,
    border = "none",
  },
  cmd = "Glow",
}
