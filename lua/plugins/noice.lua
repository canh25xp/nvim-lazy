return {
  "folke/noice.nvim",
  opts = {
    cmdline = {
      view = "cmdline",
    },
  },
  -- config = function(_, opts)
  --   vim.api.nvim_del_augroup_by_name("Dynamic cmdheight")
  --   require("noice").setup(opts)
  -- end,
}
